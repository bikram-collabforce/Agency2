# Set when building the frontend for production if the API is on a different host.
# Then the app will call this URL for /api/count instead of relative /api.
# Example: VITE_API_URL=https://api.yourdomain.com
# Leave unset when frontend and backend are same origin (e.g. Caddy proxying /api).
# VITE_API_URL=
