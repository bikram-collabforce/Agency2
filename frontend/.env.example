# Set when building for production so the app calls your backend (different host).
# Example: VITE_API_URL=https://your-backend.up.railway.app
# Leave unset for local dev (Vite proxies /api to localhost:8000).
# VITE_API_URL=
